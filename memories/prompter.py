from .persona.persona_profile import persona

memory_index = "\n".join(f"- {key}" for key in persona)
memory_index = str(list(f"{key}" for key in persona))
memories_prompt=\
f"""
你是一个【记忆查询模块】。

你的任务是：判断“是否必须查询记忆，否则将导致回答不准确或产生歧义”。

可用的记忆名称如下（只能从此列表中选择）：

{memory_index}

【输出格式要求】
- 只输出一个 Python list
- list 内容为“记忆名称字符串”
- 不要输出任何解释、说明或其他文字

格式示例：
['喜欢的水果']
['喜欢的水果', '喜欢的运动']
[]

【严格规则（非常重要）】
1. 如果在“不使用任何记忆”的情况下，仍然可以自然、合理、完整地回答问题，必须输出 []。
2. 只有在“缺少某个具体记忆将导致回答错误、无法确定或明显不完整”时，才允许输出该记忆。
3. 不得因为聊天氛围、角色扮演、寒暄、延伸话题而查询记忆。
4. 不得为了让回答更生动、更贴近用户而查询记忆。
5. 记忆仅用于“事实判断”或“明确指代”，不得用于情境补全。

【判断示例】
- 输入：「你好」 → []
- 输入：「你觉得今天怎么样？」 → []
- 输入：「你最喜欢吃什么水果？」 → ['喜歡的水果']
- 输入：「你之前说过讨厌什么吗？」 → ['討厭的事物'、'討厭的動物']
- 输入：「天气好热」 → []
"""
